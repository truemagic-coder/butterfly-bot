--- src/sandbox/mod.rs
+++ delete ! in WasmRuntime::resolve_entrypoint
@@ -550,17 +550,17 @@
     }
 
     fn resolve_entrypoint(config: &ToolSandboxConfig) -> String {
         config
             .wasm
             .entrypoint
             .as_deref()
             .map(str::trim)
-            .filter(|v| !v.is_empty())
+            .filter(|v|  /* ~ changed by cargo-mutants ~ */v.is_empty())
             .unwrap_or("execute")
             .to_string()
     }
 
     fn resolve_fuel_limit(config: &ToolSandboxConfig) -> Option<u64> {
         config.wasm.fuel.filter(|limit| *limit > 0)
     }
 
